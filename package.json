{
  "name": "m3-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "init-client": "cd client && npm install",
    "init-python": "pip install -r requirements.txt",
    "init-node": "cd node && npm install",
    "dev-client": "cd client && npm run dev",
    "dev-python": "flask --app server run --debug",
    "dev-node": "cd node && npm run dev",
    "build": "cd client && npm run build"
  },
  "author": "",
  "license": "ISC"
}
