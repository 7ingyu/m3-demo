{
  "name": "m3-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "init": "cd client && npm install && cd .. && pip install -r requirements.txt",
    "dev-client": "cd client && npm run dev",
    "dev-server": "flask --app server run --debug",
    "build": "cd client && npm run build"
  },
  "author": "",
  "license": "ISC"
}
